# پروتکل نگهداری و توسعه عظیم ابزار

این فایل برای استفاده بعد از تحویل به مالک سایت نوشته شده است.

## اصل مهم
ساختار پایه سایت نباید برای هر باگ یا قابلیت دوباره ساخته شود. منبع اصلی واقعیت همیشه این سه مورد است:

1. کد فعلی repository
2. دیتابیس/Storage/Auth فعلی Supabase
3. تنظیمات و Secretهای فعلی سرویس‌ها

برای هر تغییر، ابتدا وضعیت فعلی را بررسی کنید و سپس روی همان ساختار موجود اصلاح انجام دهید.

## وقتی مشتری می‌گوید «باگ دارد»
این اطلاعات را همراه درخواست برای توسعه‌دهنده یا AI بفرستید:

- لینک دقیق صفحه یا پنل
- توضیح دقیق کاری که انجام شد
- اتفاقی که افتاد
- اتفاقی که باید می‌افتاد
- متن کامل خطا یا اسکرین‌شات
- کد سفارش/کد محصول در صورت ارتباط با سفارش یا محصول
- مرورگر و موبایل/دسکتاپ در صورت مرتبط بودن

مثال:
«در صفحه محصولات، محصول P0001 را باز کردم، سایز 19 را انتخاب کردم؛ قیمت عوض می‌شود ولی برچسب سایز بالای کارت هنوز مقدار قبلی است.»

## وقتی قابلیت جدید می‌خواهید
نیازی به ساخت مجدد پروژه نیست. فقط بنویسید:

- چه چیزی اضافه شود
- در کدام صفحه/بخش
- رفتار دقیق چه باشد
- چه چیزهایی نباید تغییر کند
- آیا باید به دیتابیس، پنل، Telegram یا AI متصل شود

مثال:
«در پنل ادمین برای محصول، فیلد موجودی اضافه شود؛ با ذخیره محصول در products ذخیره شود و در سایت مقدار موجودی همان محصول نمایش داده شود. ظاهر دکمه‌های فعلی تغییر نکند.»

## وقتی محصول یا قیمت ویرایش می‌شود
تا جای ممکن از پنل مدیریت استفاده کنید. اطلاعات عملیاتی نباید با ویرایش مستقیم فایل‌های HTML از دیتابیس جدا شوند.

بعد از هر تغییر مهم:
- سایت را بررسی کنید
- پنل را بررسی کنید
- اگر محصول/سایز تغییر کرده، ربات محصول را هم بررسی کنید
- اگر سفارش تحت تأثیر است، مسیر سبد/ثبت سفارش/پیگیری را بررسی کنید

## فرمت درست تحویل یک باگ به AI
برای کار فنی، این موارد را کنار هم بدهید:

REPOSITORY: لینک GitHub فعلی
LIVE_SITE: لینک سایت فعلی
ADMIN: لینک پنل
ERROR: متن دقیق خطا
PAGE: صفحه‌ای که مشکل دارد
STEPS: مراحل تکرار
EXPECTED: نتیجه مورد انتظار
ACTUAL: نتیجه فعلی
CONSTRAINTS: چیزهایی که نباید تغییر کند

اگر فایل یا اسکرین‌شات خطا وجود دارد، همان فایل را هم ضمیمه کنید.

## قبل از اصلاح
AI/توسعه‌دهنده باید ابتدا کد و ساختار فعلی را بخواند، وابستگی‌ها را پیدا کند، و مشخص کند مشکل از Frontend، Supabase، Edge Function، Cloudflare یا داده است.

نباید برای یک باگ کوچک کل سیستم را دوباره پیاده‌سازی کند.

## بعد از اصلاح
باید:
- تغییر را در repository ثبت کند
- تست مربوط به همان بخش را اجرا کند
- در صورت امکان تست regression اجرا کند
- مشخص کند چه فایل‌ها و چه بخش‌هایی تغییر کرده‌اند
- اگر schema دیتابیس تغییر کرده، migration جدید ثبت کند

## قانون دیتابیس
تغییرات ساختاری دیتابیس باید migration داشته باشند. اجرای دستی SQL بدون ثبت migration باعث می‌شود نسخه repository و دیتابیس از هم جدا شوند.

## قانون Secretها
هیچ API Key خصوصی، service-role key، Telegram bot token یا secret داخل frontend یا repository قرار نگیرد.

Secretها فقط در محیط سرویس مقصد نگهداری شوند.

## قانون انتقال مالکیت
بعد از انتقال به مالک جدید، این موارد باید در اختیار او باشند:

- GitHub
- Supabase
- Cloudflare/DNS
- دامنه
- Telegram Bot
- AI provider keys
- حساب‌های مدیریتی و MFA
- backup/recovery information

پس از انتقال صحیح، تعمیرات و توسعه‌های بعدی روی همان ساختار موجود انجام می‌شوند و نیازی به ساخت مجدد معماری نیست.

## نسخه و پشتیبان
برای هر تغییر بزرگ، قبل از تغییر یک commit/tag یا backup نگه دارید.

یک بسته handover/maintenance قدیمی را جایگزین منبع زنده نکنید؛ همیشه آخرین repository و آخرین وضعیت دیتابیس مرجع هستند.

## برای درخواست بعدی از AI فقط این را اضافه کنید
«به ساختار فعلی عظیم ابزار دست نزن مگر برای رفع علت واقعی مشکل. ابتدا repository و وابستگی‌های موجود را بررسی کن، سپس کمترین تغییر لازم را اعمال کن و تست regression اجرا کن.»