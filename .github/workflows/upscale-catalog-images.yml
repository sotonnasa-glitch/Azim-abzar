name: Upscale catalog images in Supabase
on:
  push:
    branches: [main]
  workflow_dispatch:
permissions:
  contents: write
jobs:
  diagnose:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: Process all chunks and record HTTP failures
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          MIGRATOR_URL: https://lzkrwtnylkordkwkdyzp.supabase.co/functions/v1/catalog-image-migrator
        run: |
          set -u
          : > /tmp/chunks.txt
          for offset in $(seq 0 10 900); do
            body=$(mktemp)
            code=$(curl --silent --show-error --max-time 180 -o "$body" -w '%{http_code}' -H "x-github-token: $GITHUB_TOKEN" "$MIGRATOR_URL?action=upscale&offset=$offset&limit=10")
            echo "OFFSET=$offset HTTP=$code" | tee -a /tmp/chunks.txt
            cat "$body" | tee -a /tmp/chunks.txt
            rm -f "$body"
          done
          cp /tmp/chunks.txt chunks-report.txt
      - name: Persist report and remove one-time workflow
        if: always()
        shell: bash
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          git clone --depth 1 "https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git" repo-cleanup
          cd repo-cleanup
          cp ../chunks-report.txt .
          rm -f .github/workflows/upscale-catalog-images.yml
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "chore: save enhancement diagnostics and remove one-time workflow [skip ci]"
          git push origin HEAD:main
