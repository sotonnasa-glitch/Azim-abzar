# راه‌اندازی پرداخت آنلاین عظیم ابزار

این فایل برای زمانی است که صاحب فروشگاه درگاه پرداخت آنلاین را تهیه کرد.

## فعلاً چه چیزی در پروژه آماده است؟

منطق پرداخت از منطق سفارش جدا شده است:

مشتری → ثبت سفارش → سفارش با وضعیت پرداخت در انتظار → شروع پرداخت در Edge Function → درگاه → callback → verify سمت سرور → تغییر payment_status به paid

تا وقتی درگاه واقعی پیکربندی و تست نشده باشد، گزینه پرداخت آنلاین در سایت فعال نمی‌شود.

## وقتی مشتری درگاه را تهیه کرد، این اطلاعات را از او بگیر

1. نام دقیق درگاه/شرکت
2. نوع حساب درگاه (مثلاً درگاه مستقیم یا سرویس پرداخت واسط)
3. شناسه پذیرنده / Merchant ID در صورت وجود
4. API Key / Security Key / Secret در صورت وجود
5. نام متغیرهای credential اگر شرکت خودش مشخص کرده باشد
6. مستندات API یا لینک پنل/مستندات رسمی درگاه
7. آدرس callback مورد تأیید درگاه و اینکه آیا callback را در پنل باید دستی ثبت کند
8. واحد مبلغی که API درگاه می‌خواهد (ریال، تومان یا واحد دیگر)
9. روش موردنیاز برای verify/refund در صورت ارائه
10. محیط تست (Sandbox) و محیط اصلی (Production)، اگر هر دو دارد

## چه چیزهایی را نباید داخل GitHub قرار داد؟

این موارد نباید داخل HTML، JavaScript عمومی، repository یا فایل قابل دانلود قرار بگیرند:

- API Secret
- Bot Token
- Service Role Key
- هر credential خصوصی درگاه

credentialها در Secretهای Supabase Edge Function نگهداری می‌شوند.

## بعد از دریافت اطلاعات

تنها قسمت لازم که باید به کد اضافه شود، adapter همان provider است. زیرساخت سفارش و پرداخت همین حالا آماده است و نباید دوباره از صفر ساخته شود.

Adapter باید دو کار اصلی داشته باشد:

- start: مبلغ سفارش را به فرمت درگاه تبدیل کند و کاربر را به URL پرداخت بفرستد.
- verify: callback درگاه را بررسی کند، مبلغ را با مبلغ سفارش تطبیق دهد و فقط در صورت تأیید معتبر، سفارش را paid کند.

## تستی که باید انجام شود

- شروع پرداخت
- برگشت موفق از درگاه
- برگشت ناموفق/لغو پرداخت
- تطبیق مبلغ
- جلوگیری از پرداخت تکراری
- ثبت authority/reference
- تغییر صحیح وضعیت سفارش
- نمایش وضعیت پرداخت در پیگیری سفارش
- تست دوباره با سفارش دوم
- تست دسکتاپ و موبایل

## قانون مهم

هیچ وقت فقط با پارامترهای callback در مرورگر، سفارش را paid نکنید. تأیید باید سمت سرور و از طریق API verify خود درگاه انجام شود.


## لایه‌های محافظت مالی که اکنون در معماری اضافه شده‌اند

برای جلوگیری از ضرر مشتری، مدیر یا فروشگاه، وضعیت «پرداخت آنلاین» فقط از مسیر تأیید سروری درگاه قابل نهایی شدن است.

- callback مرورگر به‌تنهایی قابل اعتماد نیست.
- مبلغ تراکنش هنگام ایجاد از مبلغ سفارش همان لحظه snapshot می‌شود و پس از ایجاد تراکنش فعال، مبلغ سفارش قابل تغییر نیست.
- واحد رسمی مبلغ فروشگاه «تومان» است؛ adapter درگاه تبدیل لازم مثل تومان → ریال را انجام می‌دهد و مبلغ تأییدشده را دوباره به واحد رسمی فروشگاه برمی‌گرداند.
- برای یک سفارش، تراکنش آنلاین باز تکراری کنترل می‌شود و reference / request-id / idempotency از ثبت دوباره همان تراکنش جلوگیری می‌کنند.
- پرداخت‌های نامشخص یا مغایر به جای paid شدن، در وضعیت «نیازمند بررسی» متوقف می‌شوند.
- پرداخت موفق باید با مبلغ دقیق سفارش تطبیق داده شود.
- پس از پرداخت موفق، ثبت پرداخت و وضعیت سفارش در یک مسیر اتمیک انجام می‌شود.
- یک سفارش آنلاین تا تأیید پرداخت اجازه پردازش، بسته‌بندی یا ارسال ندارد.
- برگشت وجه نیز مستقل از «درخواست عودت» است؛ تا وقتی خود درگاه عودت را تأیید نکرده باشد، وضعیت سفارش refunded نمی‌شود.
- رویدادهای پرداخت در جدول payment_events ثبت می‌شوند تا مسیر تراکنش قابل حسابرسی باشد.
- اعلان تلگرام در صف payment_notification_queue ثبت می‌شود؛ قطع موقت تلگرام نباید باعث از دست رفتن رویداد پرداخت شود.
- برای پرداخت‌هایی که کاربر پول را پرداخت کرده ولی callback/اینترنت به مشکل خورده، Edge Function مربوط به reconcile آماده بررسی مجدد سمت سرور است.

### رفتار تلگرام ادمین

برای پرداخت آنلاین، ادمین نباید دستی «پرداخت شد» بزند. پیام تأیید از سیستم می‌آید و وضعیت بر اساس Verify واقعی درگاه تغییر می‌کند. دکمه‌های دستی پرداخت فقط برای روش‌های غیرآنلاین مثل تماس/هماهنگی فروشگاه باقی می‌مانند.

### پرداخت نامشخص

در وضعیت pending یا review_required به مشتری نباید توصیه شود دوباره پول پرداخت کند. ابتدا باید وضعیت همان تراکنش از سمت سرور روشن شود تا خطر دو بار پرداخت وجود نداشته باشد.

### بازیابی پرداخت

بعد از failed یا cancelled واقعی، مشتری می‌تواند همان سفارش را دوباره وارد درگاه کند. سفارش جدید ساخته نمی‌شود. این قابلیت برای جلوگیری از چند سفارش مشابه در اثر خطای اتصال در نظر گرفته شده است.

### تطبیق و ثبت عودت

درگاه واقعی باید علاوه بر start و verify، در صورت پشتیبانی، API refund هم داشته باشد. نتیجه refund باید با مبلغ درخواست‌شده تطبیق داده شود و فقط بعد از پاسخ قطعی درگاه، وضعیت refunded یا partially_refunded ثبت شود.

### reconcile خودکار

Edge Function azim-payment-reconcile آماده است تا تراکنش‌های initiated/pending را دوره‌ای دوباره Verify کند. پس از مشخص شدن درگاه واقعی، این تابع باید با یک Secret اختصاصی محافظت و با pg_cron + pg_net هر چند دقیقه اجرا شود. مستندات فعلی Supabase استفاده از Vault برای نگهداری credential زمان‌بندی و اجرای Edge Function را توصیه می‌کنند.
